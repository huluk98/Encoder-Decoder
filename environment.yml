name: encoder-decoder-prune
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pytorch
  - pytorch-cuda=12.1
  - pip:
      - accelerate>=0.29
      - datasets>=2.19
      - numpy>=1.24
      - pytest>=8.0
      - ruff>=0.4
      - tqdm>=4.66
      - transformers>=4.41

